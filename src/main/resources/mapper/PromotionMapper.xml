<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dndn.promotions.repository.PromotionRepository">

    <resultMap id="userVO" type="com.dndn.promotions.model.UserVO">
        <result column="id" property="id"/>
        <result column="contact" property="contact"/>
        <result column="draw_cnt" property="drawCnt"/>
    </resultMap>
    <resultMap id="userDrawResultVO" extends="userVO" type="com.dndn.promotions.model.UserDrawResultVO">
        <result column="amount" property="amount"/>
    </resultMap>

    <resultMap id="drawVO" type="com.dndn.promotions.model.DrawVO">
        <result column="id" property="id"/>
        <result column="amount" property="amount"/>
        <result column="total" property="total"/>
        <result column="winner_cnt" property="winnerCnt"/>
    </resultMap>

    <select id="getUser" resultMap="userVO" parameterType="com.dndn.promotions.model.UserVO">
        SELECT id, contact, draw_cnt
        FROM devmanta.users
        <where>
            <if test="contact != null">
                AND contact = #{contact}
            </if>
            <if test="id != null">
                AND id = #{id}
            </if>
        </where>
    </select>

    <insert id="insertUser" parameterType="com.dndn.promotions.model.UserVO" useGeneratedKeys="true" keyProperty="id">
      INSERT INTO devmanta.users(contact)
      VALUES (#{contact})
    </insert>

    <select id="getDrawResult" resultMap="userDrawResultVO">
        SELECT u.*, d.amount
        FROM devmanta.users u
             JOIN devmanta.draw_result dr ON u.id = dr.user_id
             JOIN devmanta.draw d ON dr.draw_id = d.id
        <where>
            <if test="userId != null">
                u.id = #{userId}
            </if>
        </where>
    </select>

    <insert id="insertDrawResult" parameterType="Map">
        INSERT INTO devmanta.draw_result(user_id, draw_id) VALUES (#{userId}, #{drawId})
    </insert>

    <update id="addUserDrawCntById" parameterType="com.dndn.promotions.model.UserVO">
        UPDATE devmanta.users
        SET draw_cnt = draw_cnt + 1
        WHERE id = #{id}
    </update>

    <delete id="deleteDrawResultByUserId">
        DELETE FROM devmanta.draw_result
        WHERE user_id = #{userId}
    </delete>

    <select id="getDrawtById" resultMap="drawVO">
        SELECT id, amount, total, winner_cnt
        FROM devmanta.draw
        WHERE id = #{id}
    </select>

    <update id="updateDrawWinnerCntById" parameterType="com.dndn.promotions.model.DrawVO">
        UPDATE devmanta.draw
        SET winner_cnt = #{winnerCnt}
        WHERE id = #{id}
    </update>

</mapper>